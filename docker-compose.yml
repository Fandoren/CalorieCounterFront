services:
  calorie-counter-front:
    build: .
    container_name: calorie-counter-front
    restart: unless-stopped
    ports:
      - "5173:3000"
    networks:
      - localweb

networks:
  localweb:
    external: true
